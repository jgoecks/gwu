<tool id="getreads" name="Retrieve a subset of reads">
  <description> from a FASTQ file</description>
  <command interpreter="perl">
    getReads.pl $in1 $in2 $out
  </command>
  <inputs>
    <param format="fastq" name="in1" type="data" label="FASTQ file #1, listing reads of interest" help="Used only to get the headers for the reads to be retrieved from FASTQ file #2."/>
    <param format="fastq" name="in2" type="data" label="FASTQ file #2, listing reads to be retrieved"/>
  </inputs>
  <outputs>
    <data format="fastq" name="out" label="${tool.name} on ${on_string}"/>
  </outputs>

  <help>
This tool retrieves a set of reads from a FASTQ file, using the read headers
from another FASTQ file.
  </help>

</tool>
